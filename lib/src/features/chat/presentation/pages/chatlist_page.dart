// chatlist_page.dart
import 'package:flutter/material.dart';

class ChatlistPage extends StatelessWidget {
  const ChatlistPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Chatlist Page'));
  }
}
