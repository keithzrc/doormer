import 'package:flutter/material.dart';

class ActivationPageWeb extends StatelessWidget {
  const ActivationPageWeb({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(child: Text('Activation Page'));
  }
}
